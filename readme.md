Run the backend and frontend by running ```sh launch.sh``` from the Terminal on linux or in git bash on Windows to run or, alternatively run them by running ``` docker compose up -d --build``` from the terminal. Docker and Docker Compose required.
